import java.util.Date;
import java.util.Scanner;
public class TaskOne {
public static dateFormat(){
	Scanner input = new Scanner(System.in);
	
	System.out.println("Enter your date of birth: ");

	input.replace(",", "").replace("-","").replace(".","").replace(";","").replace(" ", "")
	input = integer.parseInt(0, 2);
	newUser_day = int(userDay);
	userMonth = integer.parseInt(2, 4);
	newUserMonth = int(userMonth);
	userYear = integer.parseInt(4, 8);
	newUserYear = int(userYear);
	int today = datetime.date();
	int year = today.year;
	int month = today.momth;
	int day = today.day;
	int age = "";
	if( day < newUserDay or month < newInput){
		age = yearUserYear - 1;
	}
	else{
		age = year - newUserYear;
	System.out.print(" You are{age}years old");
	}
}

return dateFormat;

}
	